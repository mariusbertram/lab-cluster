---
# =============================================================================
# Phase 0.4 – Provision Clients on UniFi
# =============================================================================
- name: "Provision Clients on UniFi"
  hosts: localhost
  gather_facts: false
  roles:
    - role: unifi_client_config
